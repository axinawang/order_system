 <button type="primary" bindtap="toCreate"> 创建新用户 </button>
 <block wx:for="{{list}}">
    <navigator url="../buyerDetail/buyerDetail?id={{item.id}}" hover-class="navigator-hover">
    
    <view class="right">
    <text class="title">{{item.name}}</text>
    </view>
</navigator>
</block>